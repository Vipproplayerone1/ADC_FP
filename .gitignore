# Secrets
.env
.env.local

# Claude Code local state (developer-only; not part of the deliverable)
.claude/
CLAUDE.md
RECONSTRUCTION_NOTES.md

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/

# Virtual env
.venv/
venv/

# Vector DB + uploads (runtime artifacts)
vector_db/chroma/
!vector_db/chroma/.gitkeep
data/raw/uploaded_pdfs/
!data/raw/uploaded_pdfs/.gitkeep
data/processed/chunks/
!data/processed/chunks/.gitkeep

# Notebooks
.ipynb_checkpoints/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Transient logs
*.log

# Generated eval report is now committed so graders see real numbers without re-running
# (was previously gitignored — left in repo intentionally)
